1. Шаблоны функций представляют некоторый образец, по которому можно создать конкретную функцию, специфическую для определенного типа.

2. Шаблон класса позволяет задать тип для объектов, используемых в классе.

3,4. Явное определение специализации шаблонных функций – template <typname T>  и явный template <> class Print(string) и
 шаблон будет работать с типом string иначе чем с другими.

5. 